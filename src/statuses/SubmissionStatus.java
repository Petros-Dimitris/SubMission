package statuses;

public enum SubmissionStatus {

	Active, // used to indicate that data are submitted
	Inactive, // used to indicate that data are only (temporary) saved
	Cancelled; // submission was cancelled (by the submitter)

}
